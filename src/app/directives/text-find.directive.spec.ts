import { TextFindDirective } from './text-find.directive';

describe('TextFindDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFindDirective();
    expect(directive).toBeTruthy();
  });
});
